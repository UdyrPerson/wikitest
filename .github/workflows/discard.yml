name: Defausse C/PC/R (5 comptes)

# Defausse les cartes Communes/Peu Communes/Rares accumulees sur les trois
# comptes (wm_discard.py, filtre strict --rarities C,PC,R -- jamais
# SR/UR/L). Separe des workflows d'ouverture de boosters le 30/08/2026
# (etait avant une etape couplee dans auto-booster.yml / auto-booster-2.yml)
# : tourne desormais sur son propre cron, independamment du rythme
# d'ouverture. Etendu le meme jour a collecteur (3e compte, ouvre aussi
# des boosters via auto-booster-3.yml, mais n'a pas de workflow de trade
# puisqu'il est le destinataire, pas l'expediteur, dans trade.yml).
#
# Pure API (comme les autres workflows du projet), pas de navigateur.
#
# Cadence 50 min comme les workflows de boosters (voir auto-booster.yml
# pour le pourquoi des six lignes cron et le lien avec l'expiration des
# jetons Supabase), decale de +5 min par rapport au compte 1 pour tomber
# entre deux creneaux d'ouverture (les comptes occupent 0/10/20/30/40) --
# mais ce n'est pas une dependance stricte, ce workflow defausse
# simplement ce qui est present au moment ou il tourne, quelle qu'en soit
# l'origine.

on:
  schedule:
    - cron: "5 0,5,10,15,20 * * *"
    - cron: "15 4,9,14,19 * * *"
    - cron: "25 3,8,13,18,23 * * *"
    - cron: "35 2,7,12,17,22 * * *"
    - cron: "45 1,6,11,16,21 * * *"
    - cron: "55 0,5,10,15,20 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: wm-discard
  cancel-in-progress: false

jobs:
  discard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Installe Playwright (client API seulement, pas de navigateur)
        run: pip install playwright

      - name: Ecrit les sessions des cinq comptes depuis les secrets
        env:
          WM_TEST_STORAGE_STATE: ${{ secrets.WM_TEST_STORAGE_STATE }}
          WM_TEST2_STORAGE_STATE: ${{ secrets.WM_TEST2_STORAGE_STATE }}
          WM_TEST3_STORAGE_STATE: ${{ secrets.WM_TEST3_STORAGE_STATE }}
          WM_TEST4_STORAGE_STATE: ${{ secrets.WM_TEST4_STORAGE_STATE }}
          WM_TEST5_STORAGE_STATE: ${{ secrets.WM_TEST5_STORAGE_STATE }}
        run: |
          python -c "import os, pathlib; pathlib.Path('state1.json').write_text(os.environ['WM_TEST_STORAGE_STATE'])"
          python -c "import os, pathlib; pathlib.Path('state2.json').write_text(os.environ['WM_TEST2_STORAGE_STATE'])"
          python -c "import os, pathlib; pathlib.Path('state3.json').write_text(os.environ['WM_TEST3_STORAGE_STATE'])"
          python -c "import os, pathlib; pathlib.Path('state4.json').write_text(os.environ['WM_TEST4_STORAGE_STATE'])"
          python -c "import os, pathlib; pathlib.Path('state5.json').write_text(os.environ['WM_TEST5_STORAGE_STATE'])"

      - name: Jitter aleatoire (0-15s)
        run: python -c "import random, time; time.sleep(random.uniform(0, 15))"

      # continue-on-error sur chaque compte : la session Supabase expire au
      # bout d'une heure pile (expires_in: 3600, constate le 30/08/2026),
      # donc un compte peut avoir une session morte au moment du run. Sans
      # ca, le premier 401 arretait le job et les comptes suivants
      # n'etaient jamais traites -- c'est ce qui est arrive le 30/08/2026,
      # ou compte 2 a bloque la defausse de compte 4 et compte 5.
      - name: Compte 1 -> defausse C/PC/R
        continue-on-error: true
        run: python wm_discard.py state1.json --rarities C,PC,R

      - name: Compte 2 -> defausse C/PC/R
        continue-on-error: true
        run: python wm_discard.py state2.json --rarities C,PC,R

      - name: collecteur -> defausse C/PC/R
        continue-on-error: true
        run: python wm_discard.py state3.json --rarities C,PC,R

      - name: compte 4 -> defausse C/PC/R
        continue-on-error: true
        run: python wm_discard.py state4.json --rarities C,PC,R

      - name: compte 5 -> defausse C/PC/R
        continue-on-error: true
        run: python wm_discard.py state5.json --rarities C,PC,R
